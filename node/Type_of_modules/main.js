const module_fs = require('fs'); // A module with bunch of stuff that could be used to read strings in text files.
const module_http = require('http'); // Used to build an http server.
//const module_nodmon = require('nodemon')

console.log(module_fs);